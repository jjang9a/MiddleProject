<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.start.mapper.BoardMapper">
<!-- 가애 -->


<!-- 순덕 -->


<!-- 용억 -->
<select id="mateList" resultType="co.start.vo.BoardVO">
 select * from board where b_category='WI'
 </select>

<select id="selectMate" resultType="co.start.vo.BoardVO" parameterType="co.start.vo.BoardVO">
	select * from board where user_id = #{userId}
</select>


<!-- 대준 -->

<select id="scManagerList" resultType="co.start.vo.BoardVO">
	select * from board where b_category='CU'
</select>

 

<select id="scSearch" resultType="BoardVO" parameterType ="int">
  	select * from board where b_id = #{bid}
  </select>

<!-- 
 <insert id="scWrite" parameterType = "BoardVO">
  	insert into board(b_id, user_id, b_title, b_contents, b_writeDate)
  	values(board_seq.nextval, #{user_id}, #{b_title}, #{b_contents}, #{file})
  </insert>
  
  
  
  <update id="scModify" parameterType="BoardVO">
  	update board set board_title=#{bTitle}, b_contents = #{b_contents}
  	where b_id = #{b_id}
  </update>
  
  <delete id="deleteNotice" parameterType="int">
  	delete from notice where notice_id = #{b_id}
  </delete>


<select id="scUserList" resultType="BoardVO">
	select * from board where user_id = #{user_id}
</select>   -->

</mapper>
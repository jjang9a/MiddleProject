<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.start.mapper.BoardMapper">
<!-- 가애 -->


<!-- 순덕 -->


<!-- 용억 -->
<select id="mateList" resultType="co.start.vo.BoardVO">
 select * from board;

</select>

<select id ="mateListWithPaging" resultType="co.start.vo.BoardVO" parameterType="int">
<![CDATA[
	select b_id, user_id, b_title, b_writedate
	from ( select rownum rn, select b_id, user_id, b_title, b_writedate
	   from ( select select b_id, user_id, b_title, b_writedate
	   		from board
	   		order by user_id desc) a
	   	where rownum <= (#{page} * 10) ) b
	where b.rn > (#{page} - 1) * 10
	   	]]>

</select>

<select id="getTotalCount" resultType="int">
	select count(*) from board
</select>

<!-- 대준 -->


</mapper>